---
import Socials from "./Socials.astro";

const currentYear = new Date().getFullYear();

export interface Props {
  noMarginTop?: boolean;
}

const { noMarginTop = false } = Astro.props;
---

<footer
  class:list={[
    "border-border mx-auto mt-16 w-full max-w-5xl border-t px-4 sm:px-6",
    { "mt-auto": !noMarginTop },
  ]}
>
  <div
    class="flex flex-col items-center justify-between gap-4 px-4 py-6 sm:flex-row-reverse sm:px-5"
  >
    <Socials centered />
    <div
      class="text-foreground/80 my-2 flex flex-col items-center text-sm whitespace-nowrap sm:my-0 sm:flex-row"
    >
      <span class="text-accent font-semibold"
        >Copyright &#169; {currentYear}</span
      >
      <span class="hidden sm:inline">&nbsp;|&nbsp;</span>
      <span>All rights reserved.</span>
    </div>
  </div>
</footer>
