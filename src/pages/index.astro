---
import { getCollection } from "astro:content";
import Layout from "@/layouts/Layout.astro";
import Header from "@/components/Header.astro";
import Footer from "@/components/Footer.astro";
import LinkButton from "@/components/LinkButton.astro";
import Card from "@/components/Card.astro";
import getSortedPosts from "@/utils/getSortedPosts";
import { slugifyStr } from "@/utils/slugify";
import IconArrowRight from "@/assets/icons/IconArrowRight.svg";
import { SITE } from "@/config";

const posts = await getCollection("blog");

const sortedPosts = getSortedPosts(posts);
const featuredPosts = sortedPosts.filter(({ data }) => data.featured);
const recentPosts = sortedPosts.filter(({ data }) => !data.featured);
const latestPost = sortedPosts[0];

const allTags = sortedPosts.flatMap(({ data }) => data.tags ?? []);
const tagFrequency = allTags.reduce<Record<string, number>>((acc, rawTag) => {
  const tag = rawTag?.trim();
  if (!tag) {
    return acc;
  }
  acc[tag] = (acc[tag] ?? 0) + 1;
  return acc;
}, {});

const trendingTags = Object.entries(tagFrequency)
  .sort((a, b) => b[1] - a[1])
  .slice(0, 10)
  .map(([name, count]) => ({
    name,
    count,
    slug: slugifyStr(name),
  }));

// Statistics
const totalPosts = sortedPosts.length;
const totalTags = Object.keys(tagFrequency).length;
const totalFeatured = featuredPosts.length;

// Get top categories
const topCategories = Object.entries(tagFrequency)
  .sort((a, b) => b[1] - a[1])
  .slice(0, 6)
  .map(([name, count]) => ({
    name,
    count,
    slug: slugifyStr(name),
  }));


---

<Layout>
  <Header />
  <main
    id="main-content"
    data-layout="index"
    class="mx-auto w-full max-w-6xl px-4 pb-8 sm:px-6"
  >
    <!-- Hero Section -->
    <section class="relative py-12 mb-8 overflow-hidden">
      <!-- Background Effects -->
      <div class="absolute inset-0 bg-gradient-to-br from-primary/5 via-accent/5 to-transparent -z-10"></div>
      <div class="absolute top-20 right-20 w-72 h-72 bg-accent/10 rounded-full blur-3xl animate-pulse-slow -z-10"></div>
      <div class="absolute bottom-10 left-10 w-96 h-96 bg-primary/10 rounded-full blur-3xl animate-pulse-slow animation-delay-2000 -z-10"></div>
      
      <div class="text-center max-w-4xl mx-auto">
        <div class="mb-4 inline-block px-3 py-1.5 rounded-full bg-accent/10 border border-accent/20">
          <span class="text-xs font-medium text-accent">欢迎来到 Aide Hub</span>
        </div>
        
        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold mb-4 bg-gradient-to-r from-primary via-accent to-primary bg-clip-text text-transparent animate-gradient-x leading-tight">
          {SITE.title}
        </h1>
        
        <p class="text-lg sm:text-xl text-foreground/80 mb-4 leading-relaxed max-w-2xl mx-auto">
          探索 AI 工具、软件开发与技术洞察
        </p>
        
        <p class="text-sm text-foreground/60 mb-8 max-w-3xl mx-auto">
          {SITE.desc}
        </p>
        
        <div class="flex flex-wrap justify-center gap-3">
          <LinkButton
            href="/posts/"
            class="btn-primary px-6 py-2 rounded-full text-white text-sm font-medium transition-all duration-300"
          >
            开始阅读
          </LinkButton>
          <LinkButton
            href="/about/"
            class="btn-secondary px-6 py-2 rounded-full text-sm font-medium transition-all duration-300"
          >
            关于作者
          </LinkButton>
        </div>
      </div>
    </section>

    <!-- Statistics Section -->
    <section class="mb-8">
      <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
        <div class="stat-card group p-4 rounded-xl bg-gradient-to-br from-primary/10 to-primary/5 border border-primary/20 hover:border-primary/40 transition-all duration-300 hover:-translate-y-1">
          <div class="flex items-center justify-between mb-2">
            <div class="p-2 rounded-lg bg-primary/20 group-hover:scale-110 transition-transform">
              <svg class="w-5 h-5 text-primary" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z" />
              </svg>
            </div>
            <span class="text-2xl font-bold text-primary">{totalPosts}</span>
          </div>
          <h3 class="text-xs font-medium text-foreground/60">文章总数</h3>
        </div>

        <div class="stat-card group p-4 rounded-xl bg-gradient-to-br from-accent/10 to-accent/5 border border-accent/20 hover:border-accent/40 transition-all duration-300 hover:-translate-y-1">
          <div class="flex items-center justify-between mb-2">
            <div class="p-2 rounded-lg bg-accent/20 group-hover:scale-110 transition-transform">
              <svg class="w-5 h-5 text-accent" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z" />
              </svg>
            </div>
            <span class="text-2xl font-bold text-accent">{totalTags}</span>
          </div>
          <h3 class="text-xs font-medium text-foreground/60">标签数量</h3>
        </div>

        <div class="stat-card group p-4 rounded-xl bg-gradient-to-br from-orange-500/10 to-orange-500/5 border border-orange-500/20 hover:border-orange-500/40 transition-all duration-300 hover:-translate-y-1">
          <div class="flex items-center justify-between mb-2">
            <div class="p-2 rounded-lg bg-orange-500/20 group-hover:scale-110 transition-transform">
              <svg class="w-5 h-5 text-orange-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z" />
              </svg>
            </div>
            <span class="text-2xl font-bold text-orange-500">{totalFeatured}</span>
          </div>
          <h3 class="text-xs font-medium text-foreground/60">精选文章</h3>
        </div>
      </div>
    </section>

    <!-- Latest Post Highlight -->
    {latestPost && (
      <section class="mb-8">
        <div class="relative group overflow-hidden rounded-2xl bg-gradient-to-br from-primary/5 via-accent/5 to-transparent border border-white/10 hover:border-primary/30 transition-all duration-500 p-6 sm:p-8">
          <div class="absolute top-0 right-0 w-64 h-64 bg-accent/20 rounded-full blur-3xl group-hover:scale-150 transition-transform duration-700 -z-10"></div>
          
          <div class="flex items-start justify-between mb-4">
            <div class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-accent/20 border border-accent/30">
              <span class="relative flex h-1.5 w-1.5">
                <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-accent opacity-75"></span>
                <span class="relative inline-flex rounded-full h-1.5 w-1.5 bg-accent"></span>
              </span>
              <span class="text-xs font-medium text-accent">最新发布</span>
            </div>
            <time class="text-xs text-foreground/40">{new Date(latestPost.data.modDatetime ?? latestPost.data.pubDatetime).toLocaleDateString('zh-CN', { year: 'numeric', month: 'long', day: 'numeric' })}</time>
          </div>
          
          <a href={`/posts/${latestPost.id}/`} class="block group-hover:translate-x-1 transition-transform duration-300">
            <h3 class="text-2xl sm:text-3xl font-bold mb-3 group-hover:text-primary transition-colors">
              {latestPost.data.title}
            </h3>
            <p class="text-base text-foreground/70 mb-4 line-clamp-2">
              {latestPost.data.description}
            </p>
            <div class="flex flex-wrap gap-1.5 mb-4">
              {latestPost.data.tags?.slice(0, 4).map(tag => (
                <span class="px-2 py-0.5 text-xs rounded-full bg-white/5 border border-white/10 text-foreground/60">
                  {tag}
                </span>
              ))}
            </div>
            <span class="inline-flex items-center gap-2 text-sm text-primary font-medium group-hover:gap-3 transition-all">
              阅读全文 
              <IconArrowRight class="w-5 h-5" />
            </span>
          </a>
        </div>
      </section>
    )}

    <!-- Quick Navigation Categories -->
    <section class="mb-8">
      <h2 class="text-xl font-bold mb-4 flex items-center gap-2">
        <span class="text-accent">📚</span> 热门分类
      </h2>
      <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-3">
        {topCategories.map(({ name, slug, count }) => (
          <a href={`/tags/${slug}/`} class="group p-4 rounded-xl bg-white/5 border border-white/10 hover:border-accent/30 hover:bg-accent/5 transition-all duration-300 hover:-translate-y-1 text-center">
            <div class="text-xl font-bold text-accent mb-1 group-hover:scale-110 transition-transform">{count}</div>
            <div class="text-xs font-medium text-foreground/80 group-hover:text-accent transition-colors line-clamp-2">{name}</div>
          </a>
        ))}
      </div>
    </section>

    <!-- Trending Topics -->
    {trendingTags.length > 0 && (
      <section class="mt-8 mb-8">
        <div class="flex items-center justify-between mb-4">
          <h2 class="text-xl font-bold flex items-center gap-2">
            <span class="text-accent">#</span> 热门话题
          </h2>
          <a href="/tags/" class="text-xs font-medium text-foreground/60 hover:text-primary transition-colors flex items-center gap-1">
            全部标签 <IconArrowRight class="w-3 h-3" />
          </a>
        </div>
        <div class="flex flex-wrap gap-2">
          {trendingTags.map(({ name, slug, count }) => (
            <a href={`/tags/${slug}/`} class="group relative px-3 py-1 rounded-lg bg-white/5 border border-white/10 hover:border-accent/30 hover:bg-accent/5 transition-all duration-300">
              <span class="text-sm font-medium text-foreground/80 group-hover:text-accent transition-colors">
                {name}
              </span>
              <span class="ml-2 text-xs text-foreground/40 group-hover:text-accent/60">
                {count}
              </span>
            </a>
          ))}
        </div>
      </section>
    )}

    <!-- Featured Posts -->
    {featuredPosts.length > 0 && (
      <section class="mb-8 relative">
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[120%] h-[120%] bg-gradient-to-b from-transparent via-primary/5 to-transparent pointer-events-none blur-3xl -z-10"></div>
        
        <div class="flex items-end justify-between mb-4">
          <div>
            <h2 class="text-xl font-bold mb-1">精选文章</h2>
            <p class="text-sm text-foreground/60">值得反复阅读的深度内容</p>
          </div>
        </div>

        <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
          {featuredPosts.map(({ data, id }, index) => (
            <div class={`transform transition-all duration-500 hover:-translate-y-1 ${index === 0 ? 'lg:col-span-2' : ''}`}>
              <Card
                href={`/posts/${id}/`}
                frontmatter={data}
                secHeading={false}
                coverImage={index === 0 && data.ogImage ? (typeof data.ogImage === 'string' ? data.ogImage : data.ogImage.src) : undefined}
              />
            </div>
          ))}
        </div>
      </section>
    )}

    <!-- Recent Posts -->
    {recentPosts.length > 0 && (
      <section class="mb-8">
        <div class="flex items-center gap-4 mb-4">
          <h2 class="text-xl font-bold">最新发布</h2>
          <div class="h-px flex-1 bg-gradient-to-r from-border to-transparent"></div>
        </div>
        
        <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
          {recentPosts.map(
            ({ data, id }, index) =>
              index < SITE.postPerIndex && (
                <Card
                  href={`/posts/${id}/`}
                  frontmatter={data}
                  secHeading={false}
                />
              )
          )}
        </div>

        <div class="mt-8 text-center">
          <LinkButton
            href="/posts/"
            class="inline-flex items-center gap-2 px-6 py-2 text-sm rounded-full bg-surface border border-border hover:border-primary/50 hover:text-primary transition-all duration-300 shadow-sm hover:shadow-md"
          >
            浏览全部归档
            <IconArrowRight class="w-4 h-4" />
          </LinkButton>
        </div>
      </section>
    )}
  </main>
  <Footer />
</Layout>

<script>
  document.addEventListener("astro:page-load", () => {
    const indexLayout = (document.querySelector("#main-content") as HTMLElement)
      ?.dataset?.layout;
    if (indexLayout) {
      sessionStorage.setItem("backUrl", "/");
    }
  });
</script>

<style>
  /* Custom Animations & Utilities */
  .bg-surface {
    background: color-mix(in srgb, var(--color-background) 80%, transparent);
    backdrop-filter: blur(20px);
  }

  .bg-grid-pattern {
    background-image: 
      linear-gradient(to right, rgba(128, 128, 128, 0.1) 1px, transparent 1px),
      linear-gradient(to bottom, rgba(128, 128, 128, 0.1) 1px, transparent 1px);
    background-size: 40px 40px;
  }

  .animate-pulse-slow {
    animation: pulse 8s cubic-bezier(0.4, 0, 0.6, 1) infinite;
  }

  .animation-delay-2000 {
    animation-delay: 2s;
  }

  .animate-gradient-x {
    background-size: 200% 200%;
    animation: gradient-x 8s ease infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 0.3; transform: scale(1); }
    50% { opacity: 0.5; transform: scale(1.1); }
  }

  @keyframes gradient-x {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
  }

  .perspective-1000 {
    perspective: 1000px;
  }

  .btn-primary {
    background: linear-gradient(135deg, var(--color-primary), var(--color-accent));
    box-shadow: 0 4px 14px 0 rgba(var(--accent-rgb), 0.39);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(var(--accent-rgb), 0.23);
    filter: brightness(1.1);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: var(--color-foreground);
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: var(--color-foreground);
    transform: translateY(-2px);
  }

  .stat-card {
    backdrop-filter: blur(10px);
  }

  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* Dark mode adjustments */
  :global(.dark) .bg-surface {
    background: rgba(15, 23, 42, 0.6);
  }

  :global(.dark) .stat-card {
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.2), 0 2px 4px -1px rgba(0, 0, 0, 0.1);
  }

  /* Light mode adjustments */
  :global(:not(.dark)) .stat-card {
    box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -1px rgba(0, 0, 0, 0.03);
  }
</style>
